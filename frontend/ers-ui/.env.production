# Empty on purpose: in production the frontend is served by nginx from the same
# origin that proxies /api/ to the backend (see deploy/nginx/ers.conf), so requests
# can use relative paths instead of a hardcoded host.
VITE_API_BASE_URL=
